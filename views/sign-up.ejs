<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SignUp - Cipherpost</title>
</head>
<body>
  <h2>SignUp</h2>
  <form action="/sign-up" method="post">
    <label for="name">Name</label>
    <input type="text" name="name" required value="<%= user.name %>">
    <% if (errors && errors.name) {%>
      <p><%= errors.name.msg %></p>
      <% } %>
    <br>
    <label for="email">Email</label>
    <input type="email" name="email" required value="<%= user.email %>">
    <% if (errors && errors.email) { %>
      <p><%= errors.email.msg %></p>
      <% } %>
    <br>
    <label for="password">Password</label>
    <input type="password" name="password" required value="<%= user.password %>">
    <% if(errors && errors.password) { %>
      <p><%= errors.password.msg %></p>
      <% } %>
    <br>
    <label for="confirm-password">Confirm password</label>
    <input type="password" name="confirm-password" required>
    <% if (errors && errors['confirm-password']) { %>
      <p><%= errors['confirm-password'].msg %></p>
    <% } %>
    <br>
    <button type="submit">SignUp</button>
  </form>
  <p>already registered? <a href="/log-in">Login</a> instead</p>
</body>
</html>